 "use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; export default function SearchBox(){const[q,setQ]=useState("");const r=useRouter();return <form className="search" onSubmit={e=>{e.preventDefault();if(q.trim())r.push("/?q="+encodeURIComponent(q))}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Es. Sfera Ebbasta — Rockstar"/><button>Cerca</button></form>}